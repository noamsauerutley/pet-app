<head>
<title>PetMatch</title>
<style>
.favorite_pets_flex_container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  flex-flow: row wrap;
  justify-content: space-evenly;
}
.adopted_pets_flex_container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
  flex-flow: row wrap;
  justify-content: space-evenly;
}
</style>
</head>

<div class="profile_pic">
    <%= image_tag @current_user.avatar.url || "https://mobilityoi.org/wp-content/uploads/2015/06/Avatar-No-Photo-generic-300x300.jpg", width: 300 %>

   <%= content_tag :h2, @current_user.name.titleize %>
   <%= link_to 'edit profile', edit_adopter_path %>
</div>
<br><br><br>
<h2>About Me:</h2>
<p><%= @current_user.description %></p>
<br><br><br>
<h3>My Favorite Pets</h3>
<div class="favorite_pets_flex_container">
<% if @current_user.favorite_pets.length > 0 %>
    <% @current_user.favorite_pets.each do |pet| %>
     <%= link_to pet_path(pet) do %>
        <div class="flex-item-<%= pet.id %>">

        <%= content_tag :h2, pet.name.titleize %>
    
        <%= image_tag pet.image_url, width: 150, class: "img-responsive" %><br>
     <% end %>
        <%= link_to "See More About Me!", pet_path(pet) %>
        <%= button_to "remove from favorites",method: :delete %>
        </div>
        
    <% end %>
<% else %>
<p>You haven't added any pets to your favorites yet!</p>
<%= link_to "See All Pets", pets_path %>
<% end %>
</div>
<br><br>
<h3>My Adopted Pets</h3>
<div class="adopted_pets_flex_container">
<% if @current_user.adopted_pets.length > 0 %>
    <% @current_user.adopted_pets.each do |pet| %>
       <div class="flex-item-<%= pet.id %>">
        <%= content_tag :h2, pet.name.titleize %>
        <%= image_tag pet.image_url, width: 150, class: "img-responsive" %><br>
        </div>
    <% end %>
<% else %>
<p>You haven't added any pets to your favorites yet!</p>
<%= link_to "See All Pets", pets_path %>
<% end %>
</div>



